name: conin_dev
channels:
  - conda-forge
  - https://repo.anaconda.com/pkgs/main
  - https://repo.anaconda.com/pkgs/r
dependencies:
  - pyomo
  - munch
  - numpy
  - glpk
  - flake8
  #- opt_einsum # This is a requirement of pgmpy that isn't being resolved properly
  - pytest
  - pytest-cov
  - pip 
  - pip:
    - pgmpy

